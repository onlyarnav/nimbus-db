apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "nimbusdb.fullname" . }}-deployment-controller
  labels:
    app.kubernetes.io/name: deployment-controller
    {{- include "nimbusdb.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.deploymentController.replicas }}
  selector:
    matchLabels:
      app.kubernetes.io/name: deployment-controller
  template:
    metadata:
      labels:
        app.kubernetes.io/name: deployment-controller
    spec:
      containers:
        - name: deployment-controller
          image: {{ .Values.deploymentController.image }}
          imagePullPolicy: {{ .Values.global.imagePullPolicy | default "IfNotPresent" }}
          ports:

            - containerPort: {{ .Values.deploymentController.grpcPort }}
              name: grpc
            - containerPort: {{ .Values.deploymentController.metricsPort }}
              name: metrics
          env:
            - name: GRPC_PORT
              value: {{ .Values.deploymentController.grpcPort | quote }}
            - name: METRICS_PORT
              value: {{ .Values.deploymentController.metricsPort | quote }}
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: {{ include "nimbusdb.fullname" . }}-secrets
                  key: JWT_SECRET

          readinessProbe:
            httpGet:
              path: /health
              port: metrics
            initialDelaySeconds: 5
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /health
              port: metrics
            initialDelaySeconds: 10
            periodSeconds: 10
          resources:
            {{- toYaml .Values.deploymentController.resources | nindent 12 }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "nimbusdb.fullname" . }}-deployment-controller
  labels:
    app.kubernetes.io/name: deployment-controller
    {{- include "nimbusdb.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.deploymentController.grpcPort }}
      targetPort: grpc
      name: grpc
    - port: {{ .Values.deploymentController.metricsPort }}
      targetPort: metrics
      name: metrics
  selector:
    app.kubernetes.io/name: deployment-controller
