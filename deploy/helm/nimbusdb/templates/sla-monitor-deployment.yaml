apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "nimbusdb.fullname" . }}-sla-monitor
  labels:
    app.kubernetes.io/name: sla-monitor
    {{- include "nimbusdb.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.slaMonitor.replicas }}
  selector:
    matchLabels:
      app.kubernetes.io/name: sla-monitor
  template:
    metadata:
      labels:
        app.kubernetes.io/name: sla-monitor
    spec:
      containers:
        - name: sla-monitor
          image: {{ .Values.slaMonitor.image }}
          ports:
            - containerPort: {{ .Values.slaMonitor.grpcPort }}
              name: grpc
            - containerPort: {{ .Values.slaMonitor.metricsPort }}
              name: metrics
          env:
            - name: GRPC_PORT
              value: {{ .Values.slaMonitor.grpcPort | quote }}
            - name: METRICS_PORT
              value: {{ .Values.slaMonitor.metricsPort | quote }}
          readinessProbe:
            httpGet:
              path: /health
              port: metrics
            initialDelaySeconds: 5
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /health
              port: metrics
            initialDelaySeconds: 10
            periodSeconds: 10
          resources:
            {{- toYaml .Values.slaMonitor.resources | nindent 12 }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "nimbusdb.fullname" . }}-sla-monitor
  labels:
    app.kubernetes.io/name: sla-monitor
    {{- include "nimbusdb.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.slaMonitor.grpcPort }}
      targetPort: grpc
      name: grpc
    - port: {{ .Values.slaMonitor.metricsPort }}
      targetPort: metrics
      name: metrics
  selector:
    app.kubernetes.io/name: sla-monitor
