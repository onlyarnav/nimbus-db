# NimbusDB Environment Secrets & Configuration Example
# Copy to .env for local development — NEVER commit .env to git!

JWT_SECRET=nimbusdb-dev-secret-key-32-bytes-long!
DATABASE_URL=postgres://nimbus:nimbus123@localhost:5432/nimbusdb?sslmode=disable
PORT=8080
GRPC_PORT=50051
METRIC_PORT=9090
